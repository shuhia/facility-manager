import React, { FunctionComponent } from "react";
import Container from "@mui/material";

interface TournamentProps {
  tournament?: string;
}

const Tournament: FunctionComponent<TournamentProps> = (props) => {
  return <div></div>;
};

export default Tournament;
